#!groovy
//Check Cebtos8 properties
properties([disableConcurrentBuilds()])

pipeline {
agent {
label 'master'
}
options {
buildDiscarder (logRotator(numToKeepStr: '10', artifactMumToKeepStr: '10'))
timestamps()
}
stages {
stage ("First step") {
steps {
sh 'ssh root@192.168.1.35 'hostname''
}
}
}
}
